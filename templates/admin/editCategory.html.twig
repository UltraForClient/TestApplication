{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
        <div class="form-group">
            {{ form_row(form.name) }}
        </div>
        <button type="submit" class="btn btn-primary">Save</button>
        <a href="{{ path('faq-admin') }}" class="btn btn-link">Return</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
